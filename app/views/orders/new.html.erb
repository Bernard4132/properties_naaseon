
    <h1>New Order</h1>

<%= form_for(@order) do |f| %>
  <% if @order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% @order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  
   <%= render "#{@order.current_step}_step", :f => f %>


<div class="container">
  <div class="row" style="margin-top: 10px;">    
    <div class="col-lg-12">
<p><%= f.submit "Continue", :class => "btn btn-primary" %>
  <%= f.submit "Back", :class => "btn btn-light", :name => "back_button" unless @order.first_step? %></p>

 
</div>
</div>
</div>

  
<% end %>

